language: cpp

compiler:
    - clang
    - gcc

before_install:
    - sudo apt-get update

install:
    - sudo apt-get install scons
    - sudo apt-get install libpoco-dev

before_script:
    - echo "before script"

script:
    - scons -Q
    - echo "Run Unit Test." && ./stl_unittest
