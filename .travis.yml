language: cpp

compiler:
    - clang
    - gcc

before_install:
    - sudo apt-get update

install:
    - sudo apt-get install scons

before_script:
    - echo "before script"

script:
    - scons -Q
    - echo "Run Unit Test." && ./*_test
